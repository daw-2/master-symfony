{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Créer un produit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary" formnovalidate>Créer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
